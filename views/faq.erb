<!doctype html>
<html lang=ja>
  <head>
    <title>よくある質問</title>
    <%= erb :common_head %>
  </head>
  <body>

    <%= erb :navbar %>

    <%
       settings = peercast.getSettings
     %>

    <div class=container>
      <div class="page-header">
        <h1>よくある質問</h1>
      </div>

      <h2><%= jp_words "peercast.exe が無くても配信できますか？" %></h1>
      <p>はい。配信者が peercast や PeerCast Station をインストールする必要はありません。</p>

      <h2><%= jp_words "直下ノードへのリレーは何本出ますか？" %></h1>
      <p>
        現在、
        <% if settings['maxUpstreamRatePerChannel'] != 0 %>
          合計が <%= settings['maxUpstreamRatePerChannel'] %>Kbps を超えない範囲で
        <% end %>
        <% if settings['maxRelaysPerChannel'] == 0 %>際限なく<% else %><%= settings['maxRelaysPerChannel'] %>本まで<% end %>出ます。
      </p>

    </div>

    <%= erb :footer %>

  </body>
</html>
