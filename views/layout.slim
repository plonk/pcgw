doctype html
html lang="ja"
  head
    title
      | Peercast Gateway
      - if content_for? :title
        |  / 
        == yield_content(:title)
    == erb :common_head
    == yield_content(:head)
  body.bg-light
    == erb :navbar

    hr style="display: none"

    .container
      == yield

    hr style="display: none"

    == erb :footer
