<!doctype html>
<html lang=ja>
  <head>
    <title>Peercast Gateway <%= yield_content(:title).blank? ? '' : "/ " + yield_content(:title) %></title>
    <%= erb :common_head %>
    <%= yield_content(:head) %>
  </head>
  <body>
    <%= slim :navbar %>

    <hr style="display: none">

    <div class=container>
      <%= yield %>
    </div>

    <hr style="display: none">

    <%= erb :footer %>
  </body>
</html>
