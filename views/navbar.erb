<nav class="navbar navbar-expand-sm navbar-dark bg-dark" role="navigation">
  <div class="container">

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-collapse-1" aria-controls="navbar-collapse-1" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbar-collapse-1">

      <ul class="navbar-nav mr-auto">
        <% if @user %>
          <%= slim :home_tab %>
        <% end %>
        <%= slim :stats_tab %>
        <%= slim :history_tab %>
        <%= slim :users_tab %>
        <% if @user %>
          <%= slim :notice_tab %>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if @user %>

        <li class="nav-item dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" style="padding: 8px">
            <img width="34" src="<%= @user.image_https %>" alt="[<%= @user.name %>の画像]" class="img-rounded">
          </a>
          <div class="dropdown-menu" role="menu">
            <a class="dropdown-item" href="/profile/<%= h @user.id %>">
                <strong><%= h @user.name %></strong><br>
                <small class="metadata">プロフィールを表示</small></a>
            <div class="dropdown-divider"></div>
            <% if admin_view? %>
              <a class="dropdown-item" href="/admin"><span class="text-warning">管理</span></a>
              <a class="dropdown-item" href="<%= h request.path_info + '?noadmin=yes' %>">一般ユーザビュー</a>
              <div class="dropdown-divider"></div>
            <% end %>

            <a class="dropdown-item" href="/account">設定</a>
            <a class="dropdown-item" href="/logout">ログアウト</a>
          </div>
        </li>

        <li class="nav-item">
          <form action="/create" method="GET">
            <button class="btn btn-primary navbar-btn"
                    title="配信する"
                    style="padding: 0px 10px 0px 10px; margin-left: 0.5em; margin-right: 1em">
              <span><!-- visible-lg -->
                <img src="/images/megaphone.png" width="32" alt="[メガホン]">
                配信する
              </span>
            </button>
          </form>
        </li>

        <% else %>

        <li>
          <a href="/auth/twitter">
            <img alt="Sign in with Twitter" src="/sign-in-with-twitter-gray.png">
          </a>
        </li>

        <% end %>

      </ul>
    </div> <!-- /.navbar-collapse -->
  </div>
</nav>
