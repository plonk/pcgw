<!doctype html>
<html lang=ja>
  <head>
    <title>Peercast Gateway &mdash; チャンネル作成</title>
    <%= erb :common_head %>
  </head>

  <body>
    <%= erb :navbar %>

    <div class=container>
      <h1>チャンネル作成</h1>

      <% if pcgw_env == "development" %>
        <p class="text-warning">開発モード。YP&thinsp;には掲載されません。</p>
      <% end %>

      <%= erb :form %>
    </div>

    <%= erb :footer %>
  </body>
</html>
